name: Export Vocaza Quotidien - DEBUG

on:
  workflow_dispatch:  # Test manuel uniquement

jobs:
  export-to-vocaza:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install pyairtable paramiko
      
      - name: Create export script
        run: |
          cat > export_vocaza.py << 'SCRIPT_END'
          import os
          import csv
          import json
          import paramiko
          from datetime import datetime
          from pyairtable import Api
          
          # ============================================
          # CONFIGURATION
          # ============================================
          
          # Airtable
          BASE_ID = "appe55vTZRk6Ssd2w"
          TABLE_ID = "tblcACuSWYttnFQNr"
          VIEW_ID = "viwQn2IYQ5nBuXxkY"
          
          # Noms de colonnes Airtable (pas Field IDs)
          COL_PDL = "N° de PDL"
          COL_NOM = "Nom"
          COL_PRENOM = "Prenom"
          COL_NOM_ENTREPRISE = "Nom de l'entreprise"
          COL_CIVILITE = "Civilité Abonné 1"
          COL_EMAIL = "Email"
          COL_INSTALLATEUR = "Contacts Installateur"
          COL_CONFIG_CLIENT = "Champs IA Config client"
          
          # Tracking
          COL_STATUT_VOCAZA = "Statut Vocaza"
          COL_DATE_ENVOI = "Date d'envoi Vocaza"
          
          # SFTP Vocaza
          SFTP_HOST = "sunlib.enquete-en-ligne.com"
          SFTP_PORT = 12360
          SFTP_USERNAME = "SUNLIB-9180113"
          SFTP_PASSWORD = os.environ['SFTP_PASSWORD']
          AIRTABLE_TOKEN = os.environ['AIRTABLE_TOKEN']
          
          # ============================================
          # FONCTIONS
          # ============================================
          
          def get_field_value(fields, field_name, default=''):
              """Récupère la valeur d'un champ en gérant les listes"""
              value = fields.get(field_name, default)
              if isinstance(value, list):
                  return value[0] if value else default
              return value if value else default
          
          # ============================================
          # SCRIPT PRINCIPAL
          # ============================================
          
          print("=" * 60)
          print("🚀 EXPORT VOCAZA - MODE DEBUG CORRIGÉ")
          print("=" * 60)
          print(f"Date : {datetime.now().strftime('%d/%m/%Y %H:%M:%S')}")
          print()
          
          # 1. CONNEXION AIRTABLE
          print("📊 Connexion à Airtable...")
          api = Api(AIRTABLE_TOKEN)
          table = api.table(BASE_ID, TABLE_ID)
          
          # 2. RÉCUPÉRATION DES CONTACTS
          print("📊 Récupération des contacts à exporter...")
          formula = f"AND({{{COL_STATUT_VOCAZA}}}='', {{{COL_EMAIL}}}!='')"
          
          try:
              records = table.all(view=VIEW_ID, formula=formula)
              print(f"✅ {len(records)} contacts trouvés")
          except Exception as e:
              print(f"❌ Erreur Airtable : {e}")
              exit(1)
          
          if len(records) == 0:
              print("ℹ️  Aucun contact à envoyer aujourd'hui")
              print("=" * 60)
              exit(0)
          
          # 🔍 DEBUG : Afficher les 3 premiers records complets
          print()
          print("=" * 60)
          print("🔍 DEBUG - DONNÉES AIRTABLE BRUTES")
          print("=" * 60)
          
          for i, record in enumerate(records[:3]):
              print(f"\n📋 Contact {i+1} :")
              print(f"   ID : {record['id']}")
              
              # Afficher chaque champ qu'on veut extraire AVEC LES NOMS DE COLONNES
              fields = record['fields']
              print(f"   - PDL (Colonne: {COL_PDL}) : {fields.get(COL_PDL, 'VIDE')}")
              print(f"   - Nom (Colonne: {COL_NOM}) : {fields.get(COL_NOM, 'VIDE')}")
              print(f"   - Prenom (Colonne: {COL_PRENOM}) : {fields.get(COL_PRENOM, 'VIDE')}")
              print(f"   - Entreprise (Colonne: {COL_NOM_ENTREPRISE}) : {fields.get(COL_NOM_ENTREPRISE, 'VIDE')}")
              print(f"   - Civilité (Colonne: {COL_CIVILITE}) : {fields.get(COL_CIVILITE, 'VIDE')}")
              print(f"   - Email (Colonne: {COL_EMAIL}) : {fields.get(COL_EMAIL, 'VIDE')}")
              print(f"   - Installateur (Colonne: {COL_INSTALLATEUR}) : {fields.get(COL_INSTALLATEUR, 'VIDE')}")
              print(f"   - Config (Colonne: {COL_CONFIG_CLIENT}) : {fields.get(COL_CONFIG_CLIENT, 'VIDE')}")
          
          print("\n" + "=" * 60)
          
          # 3. GÉNÉRATION DU CSV
          print()
          print("📝 Génération du fichier CSV...")
          
          timestamp = datetime.now().strftime('%Y%m%d')
          filename = f"sunlib_contacts_{timestamp}.csv"
          temp_file = f"/tmp/{filename}"
          
          record_ids = []
          csv_lines = []
          
          try:
              with open(temp_file, 'w', encoding='utf-8', newline='') as csvfile:
                  writer = csv.writer(csvfile, delimiter=';', quoting=csv.QUOTE_MINIMAL)
                  
                  # En-têtes
                  headers = [
                      'N° de PDL',
                      'Nom',
                      'Prenom',
                      'Nom de l\'entreprise',
                      'Civilité Abonné 1',
                      'Email',
                      'Installateur',
                      'Champs IA Config client'
                  ]
                  writer.writerow(headers)
                  csv_lines.append(';'.join(headers))
                  
                  # Données
                  for record in records:
                      fields = record['fields']
                      
                      row = [
                          get_field_value(fields, COL_PDL),
                          get_field_value(fields, COL_NOM),
                          get_field_value(fields, COL_PRENOM),
                          get_field_value(fields, COL_NOM_ENTREPRISE),
                          get_field_value(fields, COL_CIVILITE),
                          get_field_value(fields, COL_EMAIL),
                          get_field_value(fields, COL_INSTALLATEUR),
                          get_field_value(fields, COL_CONFIG_CLIENT)
                      ]
                      
                      writer.writerow(row)
                      csv_lines.append(';'.join([str(v) for v in row]))
                      record_ids.append(record['id'])
              
              # Vérifier la taille du fichier
              file_size = os.path.getsize(temp_file)
              print(f"✅ CSV généré : {filename}")
              print(f"   Lignes : {len(records)} contacts")
              print(f"   Taille : {file_size} octets")
              print(f"   Encodage : UTF-8")
              print(f"   Séparateur : point-virgule (;)")
              
              # 🔍 DEBUG : Afficher le contenu du CSV
              print()
              print("=" * 60)
              print("🔍 DEBUG - CONTENU DU CSV GÉNÉRÉ")
              print("=" * 60)
              for i, line in enumerate(csv_lines[:4]):  # En-têtes + 3 premières lignes
                  print(f"Ligne {i} : {line}")
              print("=" * 60)
              
          except Exception as e:
              print(f"❌ Erreur génération CSV : {e}")
              import traceback
              print(traceback.format_exc())
              exit(1)
          
          # 4. UPLOAD SFTP (désactivé en mode debug)
          print()
          print("⚠️  MODE DEBUG : Upload SFTP désactivé")
          print("   Pour activer l'upload, utilisez le workflow normal")
          
          # Nettoyer le fichier temporaire
          if os.path.exists(temp_file):
              os.remove(temp_file)
          
          print()
          print("=" * 60)
          print("✅ DEBUG TERMINÉ")
          print("=" * 60)
          SCRIPT_END
      
      - name: Run export script
        env:
          AIRTABLE_TOKEN: ${{ secrets.AIRTABLE_TOKEN }}
          SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
        run: python export_vocaza.py
